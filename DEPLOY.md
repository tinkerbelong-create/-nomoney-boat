# 公開して友達と遊ぶまでの手順

サイトをインターネットに公開し、友達にリンクを送って一緒に遊べる状態にするまでの全手順です。
**かかる費用は0円**です。クレジットカードの登録も不要です。

所要時間はだいたい1〜2時間。途中で詰まったら、そのときのエラーメッセージを控えておいてください。

---

## 全体像

用意するものは4つです。

| 何 | 役割 | 費用 |
|---|---|---|
| **GitHub** | コードの置き場所＋データ取り込みの自動実行 | 無料 |
| **Supabase** | データベースとログイン機能 | 無料 |
| **Vercel** | サイトの公開 | 無料 |
| （自分のPC） | 最初の準備だけ | — |

---

## ステップ1: GitHub にコードを置く

### 1-1. アカウントを作る

[github.com](https://github.com) で無料アカウントを作ります。すでに持っていれば飛ばしてください。

### 1-2. リポジトリを作る

右上の「+」→「New repository」。

- **Repository name**: `nomoney-boat`
- **Public / Private**: **Public を選んでください**

> **なぜ Public なのか**
> データを自動で取り込む仕組み（GitHub Actions）が、Private だと月2,000分の
> 無料枠を超えてしまいます。Public なら完全無料・無制限です。
> このコードにパスワードやキーは含まれていません（そういうものは
> あとで GitHub の Secrets に別途登録します）。他人に見られて困るものはありません。
>
> どうしても Private にしたい場合は、`.github/workflows/ingest.yml` の cron を
> `*/15 23,0-12 * * *` に変えて実行回数を半分に減らしてください。

「Create repository」を押します。

### 1-3. コードをアップロードする

いちばん簡単なのは **GitHub Desktop** を使う方法です。

1. [desktop.github.com](https://desktop.github.com) からダウンロードしてインストール
2. GitHub アカウントでログイン
3. 「File」→「Clone repository」→ さきほど作った `nomoney-boat` を選ぶ
4. 保存先のフォルダが開かれるので、そこに `nomoney-boat` フォルダの**中身をすべてコピー**する
   （`README.md`、`apps`、`packages`、`supabase`、`.github` など全部）
5. GitHub Desktop に戻ると、左側に大量のファイルが並びます
6. 左下の「Summary」に `first commit` と入力して「Commit to main」
7. 上部の「Push origin」を押す

ブラウザでリポジトリを開いて、ファイルが並んでいれば成功です。

> `.github` フォルダが見当たらない場合、隠しフォルダになっています。
> Windows なら エクスプローラーの「表示」→「隠しファイル」にチェックを入れてください。
> **このフォルダは必須です。**忘れるとデータの自動取り込みが動きません。

---

## ステップ2: Supabase の設定を仕上げる

プロジェクトの作成とSQLの実行はすでに済んでいる前提です。まだなら `README.md` の手順を先に。

### 2-1. ログイン方法の設定

左メニューの **Authentication** → **Sign In / Providers** → **Email** を開きます。

| 項目 | 設定 |
|---|---|
| Enable Email provider | **オン** |
| **Confirm email** | **オフ（推奨）** |

> **Confirm email をオフにする理由**
> オンにすると、登録時に確認メールが届き、リンクを開くまでログインできません。
> ところが Supabase の無料プランはメール送信が**1時間に数通まで**に制限されています。
> 友達数人が同時に登録しようとすると、後半の人にメールが届かず登録できません。
>
> オフにすると、メールアドレスとパスワードを入れた瞬間にアカウントが作られます。
> 身内だけで遊ぶサイトなので、これで困ることはありません。
> （メールアドレスがアカウントの識別子になる点は変わりません）

「Save」を押します。

### 2-2. サイトのURLを登録する

Vercel でのデプロイがまだなので、**ステップ3のあとで戻ってきます。**
いまは飛ばして構いません。

---

## ステップ3: Vercel でサイトを公開する

### 3-1. アカウントを作る

[vercel.com](https://vercel.com) で「Sign Up」→「Continue with GitHub」。
GitHub アカウントで入れます。

### 3-2. プロジェクトを作る

1. 「Add New...」→「Project」
2. さきほどの `nomoney-boat` の横の「Import」を押す
3. **設定画面で次を変更します（重要）**

| 項目 | 入れる値 |
|---|---|
| **Root Directory** | 「Edit」を押して **`apps/web`** を選ぶ |
| Framework Preset | Next.js（自動で入るはず） |

> Root Directory の変更を忘れると必ず失敗します。
> このプロジェクトはフォルダが分かれているので、サイト本体の場所を教える必要があります。

4. **Environment Variables** を開いて、次の3つを追加します

| Name | Value |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase の Project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase の `anon` `public` キー |
| `NEXT_PUBLIC_SETTLE_DELAY_MIN` | `15` |

Supabase の値は、Supabase の左メニュー **Project Settings → API** にあります。

> `service_role` キーのほうは**絶対にここに入れないでください**。
> あれはブラウザに送られてはいけないキーです。GitHub 側にだけ登録します。

5. 「Deploy」を押す

2〜3分待つと「Congratulations」と出て、
`https://nomoney-boat-xxxx.vercel.app` のようなURLがもらえます。**これが公開URLです。**

### 3-3. Supabase にURLを教える

Supabase に戻って、左メニュー **Authentication** → **URL Configuration**。

| 項目 | 入れる値 |
|---|---|
| **Site URL** | `https://あなたのURL.vercel.app` |
| **Redirect URLs** | `https://あなたのURL.vercel.app/**` |

Redirect URLs は「Add URL」で追加します。末尾の `/**` を忘れないでください。
パスワード再設定のリンクがここを通ります。

「Save」を押します。

---

## ステップ4: データの自動取り込みを動かす

サイトは公開できましたが、まだレースが1つも入っていません。

### 4-1. GitHub に鍵を登録する

GitHub のリポジトリ → **Settings** → 左メニュー **Secrets and variables** → **Actions**。

**「Secrets」タブ**で「New repository secret」を2回押して、次を登録します。

| Name | Value |
|---|---|
| `SUPABASE_URL` | Supabase の Project URL |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase の **`service_role`** キー |

> `service_role` キーは Supabase の Project Settings → API にあります。
> 「Reveal」を押すと表示されます。**これは管理者用の鍵**なので、
> GitHub の Secrets 以外の場所（チャット、メモ、コードの中）には置かないでください。

**「Variables」タブ**に切り替えて、「New repository variable」で1つ登録します。

| Name | Value |
|---|---|
| `INGEST_USER_AGENT` | `NoMoneyBoat/1.0 (+https://あなたのURL.vercel.app/about)` |

これはボートレース公式サイトに対する名乗りです。何かあったときの連絡先になります。

### 4-2. 初回の取り込みを手で実行する

GitHub リポジトリの **Actions** タブを開きます。

1. 左側の **daily** をクリック
2. 右側の「Run workflow」→ 緑の「Run workflow」を押す
3. 1〜2分待つ。緑のチェックが付けば成功

これで当日と翌日のレースがデータベースに入ります。

> 赤い×が出た場合は、その実行をクリックすると詳しいログが見られます。
> よくある原因は Secrets の入力ミスです。

以降は**15分ごとに自動で動く**ので、何もしなくて大丈夫です。

---

## ステップ5: 自分でひととおり試す

公開URLをスマホで開いて、次を確認してください。

1. **新規登録**タブでメールアドレスとパスワード（8文字以上）を入れる
2. ユーザーIDと表示名を決める
3. 持ちポイントが **50,000pt** になっている
4. 「レース」タブに今日のレースが並んでいる
5. レースを開いて、賭け式を選び、艇を選ぶ。**オッズが表示される**
6. 投票する。ポイントが減る
7. そのレースの発走時刻を過ぎると「締め切りました」に変わる
8. さらに15分ほど待つと、結果とポイントが反映される

**7と8が確認できて初めて「動いている」と言えます。**
友達を呼ぶ前に、必ず1レース分は最後まで確認してください。

---

## ステップ6: 友達を招待する

### あなたがやること

友達に**公開URLを送るだけ**です。招待コードのような仕組みはありません。

こんな文面で送るとスムーズです。

> ボートレースの予想で遊ぶサイト作った。お金は一切かからないよ。
> ポイントで遊ぶだけで、換金とかは一切できない仕組みにしてある。
>
> https://あなたのURL.vercel.app
>
> 1. 「新規登録」からメールアドレスとパスワードで登録
> 2. ユーザーIDを決める（フレンド検索に使うので教えて）
> 3. 毎月1日に50,000ポイントもらえる
>
> 登録したらユーザーID教えて。こっちから友達申請するね。
> 自分のIDは `あなたのユーザーID` です。

### 友達がやること

1. リンクを開く
2. 「新規登録」タブで、メールアドレスとパスワード（8文字以上）を入力
3. ユーザーIDと表示名を決める
4. マイページ → フレンド → 「フレンドを探す」であなたのユーザーIDを検索して申請

### フレンドの成立

**申請と承認の両方が必要です。**どちらかが申請し、もう一方が承認すると成立します。
承認待ちの申請はマイページ → フレンドに出ます。

フレンドになると、ホーム画面のランキングにお互いが並びます。
グループを作る必要はありません。**フレンド一覧がそのまま対戦相手**です。

> 3人以上で遊ぶ場合、全員が全員と相互にフレンドになる必要があります。
> A・B・C の3人なら、A-B、A-C、B-C の3組です。

---

## よくあるつまずき

### レースが1件も出てこない

Actions タブで `daily` が成功しているか確認してください。
成功しているのに出てこない場合は、その日たまたま開催がない可能性もあります（稀ですが）。

### 「ログインしてください」から進めない

Supabase の **URL Configuration** で Site URL と Redirect URLs が
公開URLと一致しているか確認してください。ここのミスが最も多いです。

### 結果が反映されない

- Actions タブで `ingest` が15分ごとに走っているか確認
- Public リポジトリになっているか確認（Private だと無料枠切れで止まります）
- それでも溜まった場合は、Actions から `daily` を手動実行すると回収されます

### 友達が検索で見つからない

ユーザーIDは**半角英数字と `_` のみ**です。大文字で登録しようとすると弾かれます
（自動で小文字になります）。検索は小文字で試してください。

### メールが届かない

Confirm email をオフにしていれば、登録にメールは不要です。
パスワード再設定のメールだけは送信されますが、無料プランでは
1時間に数通の制限があります。届かない場合は少し待ってから再送してください。

---

## 運用中に覚えておくこと

| やること | いつ | どうやって |
|---|---|---|
| 何もしない | 普段 | 自動で動きます |
| ポイントの配布 | 毎月1日 | 自動（`daily.yml` が実行） |
| シーズンの追加 | 年に1回程度 | `supabase/seed.sql` の `seasons` に翌年の月を追加してSQL実行 |

**シーズンだけは期限があります。** 現在 2026年8月〜10月分しか入っていません。
11月になる前に、Supabase の SQL Editor で次を実行してください。

```sql
insert into seasons (code, starts_at, ends_at, grant_amount) values
  ('2026-11', '2026-11-01 00:00:00+09', '2026-12-01 00:00:00+09', 50000),
  ('2026-12', '2026-12-01 00:00:00+09', '2027-01-01 00:00:00+09', 50000)
on conflict (code) do nothing;
```

シーズンが切れると投票できなくなるので、ここだけ気をつけてください。

---

## 忘れないでほしいこと

このサイトは**換金性がないこと**が大前提です。

- ポイントを現金・電子マネー・景品と交換する機能は作らないでください
- ポイントを売買したり、ユーザー間で譲渡できるようにしないでください
- 実際の舟券購入サイトへのリンクを置かないでください

これらを守っている限り、友達同士でポイントを競うだけの遊びです。
一つでも崩すと話が変わってくるので、そこだけは動かさないでください。
