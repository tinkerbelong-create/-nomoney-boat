{
  "name": "nomoney-boat",
  "private": true,
  "version": "0.1.0",
  "description": "ボートレースのノーマネー予想対戦サイト",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts",
    "ingest": "npm run start -w @nmb/ingest --",
    "dev": "npm run dev -w @nmb/web"
  },
  "engines": {
    "node": ">=20"
  }
}
